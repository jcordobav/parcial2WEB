import { Col, Container, Row } from "react-bootstrap";
import "./Galery.css";

export default function NoteCard({ title, createdDate, noteLogo }) {
  const cardStyle = {
    width: "18rem",
  };

  return (
    <div class="card" style={cardStyle}>
      
    </div>
  );
}
